#
# /etc/sysctl.conf - Configuration file for setting system variables
# See /etc/sysctl.d/ for additional system variables.
# See sysctl.conf (5) for information.
#

#kernel.domainname = example.com

# Uncomment the following to stop low-level messages on console
#kernel.printk = 3 4 1 3

###################################################################
# Functions previously found in netbase
#

# Uncomment the next two lines to enable Spoof protection (reverse-path filter)
# Turn on Source Address Verification in all interfaces to
# prevent some spoofing attacks
#net.ipv4.conf.default.rp_filter=1
#net.ipv4.conf.all.rp_filter=1

# Uncomment the next line to enable TCP/IP SYN cookies
# See http://lwn.net/Articles/277146/
# Note: This may impact IPv6 TCP sessions too
#net.ipv4.tcp_syncookies=1

# Uncomment the next line to enable packet forwarding for IPv4
#net.ipv4.ip_forward=1

# Uncomment the next line to enable packet forwarding for IPv6
#  Enabling this option disables Stateless Address Autoconfiguration
#  based on Router Advertisements for this host
#net.ipv6.conf.all.forwarding=1


###################################################################
# Additional settings - these settings can improve the network
# security of the host and prevent against some network attacks
# including spoofing attacks and man in the middle attacks through
# redirection. Some network environments, however, require that these
# settings are disabled so review and enable them as needed.
#
# Do not accept ICMP redirects (prevent MITM attacks)
#net.ipv4.conf.all.accept_redirects = 0
#net.ipv6.conf.all.accept_redirects = 0
# _or_
# Accept ICMP redirects only for gateways listed in our default
# gateway list (enabled by default)
# net.ipv4.conf.all.secure_redirects = 1
#
# Do not send ICMP redirects (we are not a router)
#net.ipv4.conf.all.send_redirects = 0
#
# Do not accept IP source route packets (we are not a router)
#net.ipv4.conf.all.accept_source_route = 0
#net.ipv6.conf.all.accept_source_route = 0
#
# Log Martian Packets
#net.ipv4.conf.all.log_martians = 1
#

###################################################################
# Magic system request Key
# 0=disable, 1=enable all, >1 bitmask of sysrq functions
# See https://www.kernel.org/doc/html/latest/admin-guide/sysrq.html
# for what other values do
#kernel.sysrq=438

vm.swappiness=20


net.core.rmem_max = 16777216
net.core.wmem_max = 16777216
net.core.rmem_default = 262144
net.core.wmem_default = 262144

net.ipv6.conf.all.disable_ipv6 = 1


# /etc/sysctl.conf - Optimized System Parameters for Better Performance
#---------------------------------------------------------------
# Apply sysctl settings to improve network, memory, disk, and CPU performance
# after editing this file, run: sudo sysctl -p to apply changes

# ====================
# NETWORK SETTINGS
# ====================

# Increase maximum buffer size for receive and send
# (TCP socket buffers)
net.core.rmem_max = 16777216
net.core.wmem_max = 16777216

# Default buffer size for TCP connections
net.core.rmem_default = 262144
net.core.wmem_default = 262144

# Set the minimum buffer size for UDP traffic
net.ipv4.udp_rmem_min = 4096
net.ipv4.udp_wmem_min = 4096

# Maximum number of connections that can be queued
net.core.somaxconn = 1024

# Enable TCP window scaling for improved throughput
net.ipv4.tcp_rmem = 4096 87380 16777216
net.ipv4.tcp_wmem = 4096 65536 16777216

# Enable TCP timestamps (reduces latency for long-distance communication)
net.ipv4.tcp_timestamps = 1

# Disable TCP Delayed Acknowledgments (helps reduce latency)
#net.ipv4.tcp_delayed_ack = 0

# Disable IPv6 (if not needed) – reduces system overhead
net.ipv6.conf.all.disable_ipv6 = 1

# Enable IP forwarding (useful for routers, gateway setups)
net.ipv4.ip_forward = 1

# ====================
# MEMORY SETTINGS
# ====================

# Increase the maximum virtual memory (overcommitting) to avoid memory allocation failure
vm.overcommit_memory = 1

# Allow huge pages for performance optimization (improves I/O)
vm.nr_hugepages = 128

# Optimize Swapping
# Set swappiness to 20 to reduce swap usage
vm.swappiness = 20

# Enable zswap to compress swap data and improve I/O performance
vm.swappiness = 20
#vm.zswap.enabled = 1

# ====================
# CPU SETTINGS
# ====================

# Set CPU governor to "performance" mode (disables CPU frequency scaling)
# This ensures your CPU runs at max frequency and doesn't throttle down
# Requires cpufrequtils installed
# sudo cpufreq-set -g performance
# (Alternatively, you can set this manually or through system startup scripts)
#kernel.sched_child_runs_first = 0

# ====================
# FILE SYSTEM & I/O SETTINGS
# ====================

# Mount options for root filesystem to reduce disk I/O
# Disabling atime updates to improve disk performance
# barrier=0 to avoid write barriers (SSD optimization)
fs.aio-max-nr = 1048576

# ====================
# DISK PERFORMANCE SETTINGS
# ====================

# Use deadline or noop scheduler for SSD performance improvement
# Example: echo deadline | sudo tee /sys/block/sda/queue/scheduler
# Requires system reboot or remount after changing
# Modify I/O scheduler to reduce latency for SSDs or fast disk subsystems

# ====================
# DISK TRIM (SSD)
# ====================

# Enable fstrim on SSD for automatic trimming (cleanup unneeded space)
# This ensures the SSD keeps optimal performance.
# You can enable this as a cron job or use systemd timer
# Example: sudo systemctl enable fstrim.timer
# Example for cronjob: `0 1 * * * root fstrim /`

# ====================
# SYSTEM PERFORMANCE TUNING
# ====================

# Increase file descriptor limits
# Useful for servers with high connections (web servers, DBs, etc.)
fs.file-max = 100000

# Limit the maximum number of open file descriptors for each process
fs.nr_open = 1048576

# Limit the maximum number of inotify watches (for monitoring file changes)
fs.inotify.max_user_watches = 1048576

# ====================
# TCP SETTINGS FOR HIGH PERFORMANCE NETWORK
# ====================

# Enable TCP fast open (reduces latency for establishing TCP connections)
net.ipv4.tcp_fastopen = 3

# ====================
# LOGGING & DEBUGGING
# ====================

# Turn off kernel core dumps to save space (optional)
kernel.core_pattern = /dev/null

# ====================
# OTHER PERFORMANCE TWEAKS
# ====================

# Disable IPv6 support for additional performance if not needed
# Disable if you are not using IPv6 networking to free up resources
net.ipv6.conf.all.disable_ipv6 = 1

# Apply the following settings for better web server performance
# Increase maximum number of file descriptors for server applications
# Increase connection tracking table sizes (helps with web server scalability)
net.netfilter.nf_conntrack_max = 262144

# Enable TCP keep-alive (helps in persistent connection scenarios)
net.ipv4.tcp_keepalive_time = 600

